{
  "name": "iyi-studio",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 3000 --host",
    "build": "vite build",
    "preview": "vite preview --port 3000 --host",
    "update-projects": "node update-projects.js"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  }
}
